# Delivery usage

After internal approval, run `code-composer-delivery`, verify the manifest, reference mix, MIDI parsing and stem alignment, then send the delivery directory to the external collaborator.
