# Claude plugin adapter source

This directory stores platform metadata only. `tools/build_plugins.py` creates the installable plugin by copying the canonical `skills/code-composer/` directory into `skills/code-composer/` under the plugin artifact.

Do not hand-edit a duplicate skill here; `skills/code-composer/` at repository root is the authority.
